Provisions a single Docker container as a module.

### features
- Suited for simple services with configurable image, ports, environment, volumes, and restart policy.

#### notes
- Exposes the container as resource `this` for easy reference (resource is named `this` by convention).
